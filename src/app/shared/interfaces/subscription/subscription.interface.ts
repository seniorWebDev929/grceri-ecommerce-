export interface ISubscription {
  amount: number,
  billingScheme: string,
  currency: string,
  planDescription: string,
  planId: string,
  planInterval: string,
  productId: string,
  productName: string
}
